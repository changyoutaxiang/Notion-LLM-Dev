# Notion-LLM 简化版 - 核心依赖包
# 专注于Notion+LLM处理和基础RAG功能

# 核心Web框架（云端服务用）
Flask==2.3.3
gunicorn==21.2.0

# HTTP请求库（调用LLM API和Notion API）
requests==2.31.0

# GUI框架（本地版本用）
# tkinter - Python内置模块，无需安装

# 环境变量处理
python-dotenv==1.0.0

# 时间处理
python-dateutil==2.8.2